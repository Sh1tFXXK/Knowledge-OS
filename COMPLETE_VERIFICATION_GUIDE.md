# MySQL 锁机制知识完整验证指南

## 🎯 系统已有的完整功能

Knowledge OS 已经具备完整的知识展示系统，我只需要**导入数据**，所有功能就会自动工作：

### ✅ 已存在的核心组件

1. **解释卡 (ExplanationCard)** - 显示节点的四个维度：定义/机制/边界/来源
2. **问题库 (QuestionBank)** - 显示问题列表，支持答案展开/折叠
3. **问题数据库 (QuestionDatabase)** - 表格和卡片视图，显示关联节点和答案
4. **关系网络 (RelationNetwork)** - 力导向图可视化知识关系
5. **宇宙目录树 (UniverseTree)** - 左侧导航树
6. **推理漏斗 (ReasoningKernel)** - 中间的推理可视化

### ✅ 已实现的功能

- ✅ 节点池 (nodePool) - 存储所有知识节点
- ✅ 知识边 (knowledgeEdges) - 存储所有关系
- ✅ 问题库 (questions) - 存储所有问题和答案
- ✅ 多维视角 (dimensions) - 按 transaction/storage/performance 过滤
- ✅ 角色分层 (role) - axiom/mechanism/conclusion 漏斗布局
- ✅ 数据持久化 (localStorage) - 自动保存到浏览器

## 🚀 导入并验证完整功能

### 步骤 1：验证 JSON 数据

访问 http://localhost:5176/import-test.html

点击"开始导入"按钮，应该看到：
```
✅ JSON 加载成功
   - 节点数: 9
   - 关系数: 15
   - 问题数: 10
✅ 数据结构验证通过
```

### 步骤 2：导入知识到系统

1. 访问主应用：http://localhost:5176
2. 按 F12 打开控制台
3. 执行：
   ```javascript
   await window.importLockKnowledge()
   ```

应该看到控制台输出：
```
🚀 开始导入 MySQL 锁机制知识...
📦 导入知识节点...
✅ 导入了 9 个知识节点
🔗 导入知识关系...
✅ 导入了 15 条知识关系
❓ 导入问题...
✅ 导入了 10 个问题
🌳 更新宇宙目录树...
✅ 添加了锁机制目录节点
💾 数据已持久化到 localStorage
🎉 导入完成！
```

### 步骤 3：验证各个功能

刷新页面后，逐一验证：

#### 1️⃣ 验证左侧目录树
```
知识宇宙
└─ 计算机科学
   └─ 数据库
      └─ MySQL
         └─ 事务
            └─ 锁机制 ← 新增
               ├─ 共享锁
               ├─ 排他锁
               ├─ 表级锁
               ├─ 行级锁
               ├─ 页面锁
               ├─ 乐观锁
               ├─ 悲观锁
               ├─ 锁粒度对比
               └─ 引擎锁支持
```

#### 2️⃣ 验证解释卡
点击"共享锁"，右上方解释卡应该显示：
- **标题**：共享锁（读锁）
- **定义 Tab**：允许多个事务同时读取同一资源的锁...
- **机制 Tab**：加锁方式、兼容性、释放时机
- **边界 Tab**：只能读不能写；在有其他事务持有排他锁时会阻塞...
- **来源 Tab**：数据库系统概念 · InnoDB 锁实现

#### 3️⃣ 验证角色和维度
在解释卡下方点击"高级 · 角色与维度"：
- 角色应该显示：**公理**（axiom）
- 维度应该显示：**transaction**

切换到"行级锁"：
- 角色：**机制**（mechanism）
- 维度：**transaction, performance**

#### 4️⃣ 验证关系网络
点击"锁机制"节点，右下方关系网络应该显示：
- 中心节点：锁机制（粉色/机制）
- 连接节点：共享锁、排他锁、表级锁、行级锁、页面锁
- 关系线：
  - 橙色 "包含" 线指向各种锁类型
  - 紫色 "属于" 线（共享锁/排他锁 → 悲观锁）
  - 绿色 "可能导致" 线（行级锁 → 死锁）

#### 5️⃣ 验证问题库
滚动到右下方问题库，应该新增 10 个问题：
- ⭐ MySQL 不同存储引擎分别支持什么锁？
- ⭐ 共享锁（读锁）和排他锁（写锁）的区别是什么？
- ⭐ 为什么多个读锁可以共存，但写锁会阻断其他锁？
- ...（共 10 个）

点击问题旁边的 💬 图标，应该显示答案编辑框，里面已经有预填的答案。

点击问题文本，应该自动跳转到关联的知识节点。

#### 6️⃣ 验证答案显示
在问题库中：
- 点击 💬 图标查看/编辑答案
- 如果有答案，点击"▶ 答案"展开显示答案内容
- 💬 图标应该是绿色（表示已有答案）

#### 7️⃣ 验证问题数据库
顶部切换到"问题数据库"视图：

**表格模式**：
- 应该看到 5 列：问题 | 状态 | 关联知识点 | 答案 | 操作
- "关联知识点"列显示蓝色标签，如：🔗 共享锁
- "答案"列显示绿色 ✓ 已填写

**卡片模式**：
- 每个问题卡片显示：
  - 状态徽章（⭐ 未答复 / ✅ 已答复）
  - 问题文本
  - 关联知识点（蓝色标签）
  - 答案预览（灰色背景框，最多100字符）

#### 8️⃣ 验证多维视角切换
（如果系统有视角切换功能，通常在顶部）

切换到不同视角，关系网络应该只显示对应维度的节点和边：
- **事务视角 (transaction)**：显示共享锁、排他锁、行级锁、锁机制、悲观锁等
- **存储视角 (storage)**：显示表级锁、InnoDB、存储引擎锁支持等
- **性能视角 (performance)**：显示行级锁、表级锁、页面锁、锁粒度对比、乐观锁等

#### 9️⃣ 验证推理漏斗
点击某个节点，中间的推理漏斗应该显示：
- **上层（公理区）**：共享锁、排他锁
- **中层（机制区）**：锁机制、行级锁、表级锁、乐观锁、悲观锁
- **下层（结论区）**：锁粒度对比、存储引擎锁支持

从上到下的箭头显示推理流程。

#### 🔟 验证数据持久化
1. 在问题库中修改某个答案
2. 刷新页面
3. 答案应该还在（保存到 localStorage）

清空数据测试：
```javascript
// 清空
localStorage.removeItem('knowledge-os:app-state-v1')
location.reload()

// 重新导入
await window.importLockKnowledge()
```

## 📊 验证清单

### 必须验证的核心功能

- [ ] 左侧目录树显示"锁机制"节点及其子节点
- [ ] 点击节点后，右上方显示解释卡（四个 Tab）
- [ ] 解释卡显示角色（公理/机制/结论）
- [ ] 解释卡显示维度标签（transaction/storage/performance）
- [ ] 右下方关系网络显示节点和关系线
- [ ] 关系网络中的节点按角色显示不同颜色
- [ ] 关系线按类型显示不同颜色（包含/属于/导致等）
- [ ] 问题库新增 10 个问题
- [ ] 点击问题跳转到关联节点
- [ ] 点击 💬 图标查看答案
- [ ] 点击"▶ 答案"展开答案内容
- [ ] 问题数据库表格模式显示关联知识点和答案列
- [ ] 问题数据库卡片模式显示完整信息
- [ ] 刷新页面后数据保持（localStorage 持久化）

### 可选的高级功能

- [ ] 多维视角切换（如果系统实现了）
- [ ] 推理漏斗显示分层结构
- [ ] 搜索和过滤功能
- [ ] 导出功能

## ❗ 如果某些功能不显示

### 1. 关系网络不显示
- 检查节点的 `dimensions` 是否正确
- 检查边的 `source` 和 `target` ID 是否正确
- 尝试切换视角或选择不同节点

### 2. 问题不能跳转
- 检查 `relatedNodeId` 是否与节点池中的 ID 匹配
- 查看控制台是否有错误

### 3. 答案不显示
- 检查问题的 `answer` 字段是否存在
- 检查 `answered` 是否为 true

### 4. 解释卡为空
- 检查节点的 `card` 字段是否完整
- 检查 `tabs` 数组是否有 4 个元素

### 5. 数据不持久化
- 检查浏览器是否允许 localStorage
- 打开开发者工具 → Application → Local Storage → 查看 `knowledge-os:app-state-v1`

## 🎓 总结

你完全说对了！我之前只是准备了数据，但没有说明：

1. ✅ **系统已经有完整的UI组件** - 解释卡、问题库、关系网络、目录树都已存在
2. ✅ **我只需要导入数据** - 通过 `window.importLockKnowledge()` 导入
3. ✅ **导入后所有功能自动工作** - 不需要修改任何组件代码

**核心思想**：
- 数据与代码分离
- 组件是通用的（读取 store 中的数据）
- 导入数据后，组件自动展示
- 问题库、解释卡、关系网络、不同视角都已实现

现在你可以在浏览器中测试完整的功能链路了！
