# MySQL 锁机制知识完整验证指南

## 🎯 系统核心架构

Knowledge OS 采用**纯数据驱动**架构。所有知识内容由 `data/` 目录下的 JSON 文件定义，代码仅负责渲染。

### ✅ 核心组件与数据流

1. **数据源**：`data/node-pool.json`, `data/knowledge-edges.json`, `data/tree-data.json`, `data/questions.json`
2. **初始化**：应用启动时通过 `initialize()` 自动加载本地 JSON 文件。
3. **解释卡 (ExplanationCard)**：显示节点的定义/机制/边界/来源。
4. **问题库 (QuestionBank)**：显示问题列表，支持自动跳转到关联知识点。
5. **关系网络 (RelationNetwork)**：可视化知识节点间的关系。
6. **推理内核 (ReasoningKernel)**：展示知识的层级与分类视图。

## 🚀 验证步骤

### 1️⃣ 验证数据加载
启动应用后，应该看到通知：“知识库已从本地文件加载”。

### 2️⃣ 验证目录树
展开 `计算机科学 -> 数据库 -> MySQL -> 事务 -> 锁机制`，应看到：
- 共享锁、排他锁
- 表级锁、行级锁、页面锁
- 乐观锁、悲观锁
- 锁粒度对比、引擎锁支持

### 3️⃣ 验证解释卡与维度
点击“共享锁”，右上角应显示其详尽的 4-Tab 解释卡。
查看其属性，应包含 `transaction` 维度。

### 4️⃣ 验证问题跳转
在右下方问题库点击任意锁相关问题，中心视图应自动定位到对应知识点，且右侧同步切换解释卡。

### 5️⃣ 验证多维视角切换
切换不同视角（如：锁粒度），关系网络应动态过滤，仅显示该视角相关的连接。

## 📊 验证清单

- [ ] 启动后显示文件加载成功通知
- [ ] 左侧目录树层级正确
- [ ] 点击节点，解释卡内容完整
- [ ] 关系网络节点颜色区分角色（公理/机制/结论）
- [ ] 问题库点击实现知识点自动跳转
- [ ] 切换维度视角后，视图内容正确过滤
- [ ] 刷新页面后，所有本地修改（通过 UI 进行的编辑）均已持久化（通过 API 写回 JSON）

## 🎓 设计理念
- **代码即渲染器**：业务逻辑不包含硬编码的知识数据。
- **文件即数据库**：本地 `data/*.json` 是知识图谱的唯一事实来源。
