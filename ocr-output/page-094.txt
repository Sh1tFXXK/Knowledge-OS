第8章对象·77
（续）
命令
intset编码的实现方法
hashtable编码的实现方法
调用intsetLen函数，返回整数集合所
调用dictsize函数，返回字典所包含的
SCARD
包含的元素数量，这个数量就是集合对象所
键值对数量，这个数量就是集合对象所包含
包含的元素数量
的元素数量
调用intsetFind函数，在整数集合中
调用dictFind函数，在字典的键中查找
SISMEMBER
查找给定的元素，如果找到了说明元素存在
给定的元素，如果找到了说明元素存在于集
于集合，没找到则说明元素不存在于集合
合，没找到则说明元素不存在于集合
遍历整个整数集合，使用intsetGet函
遍历整个字典，使用dictGetKey函数返
SMEMBERS
数返回集合元素
回字典的键作为集合元素
调用intsetRandom函数，从整数集合
调用dictGetRandomKey函数，从字典
SRANDMEMBER
中随机返回一个元素
中随机返回一个字典键
调用intsetRandom函数，从整数集合
调用dictGetRandomKey函数，从字典中
中随机取出一个元素，在将这个随机元素返
随机取出一个字典键，在将这个随机字典键的
SPOP
回给客户端之后，调用intsetRemove函
值返回给客户端之后，调用dictDelete函数，
数，将随机元素从整数集合中删除掉
从字典中删除随机字典键所对应的键值对
调用intsetRemove函数，从整数集合
调用dictDelete函数，从字典中删除所
SREM
中删除所有给定的元素
有键为给定元素的键值对
8.6
有序集合对象
有序集合的编码可以是ziplist或者skiplist。
ziplist编码的压缩列表对象使用压缩列表作为底层实现，每个集合元素使用两个紧
挨在一起的压缩列表节点来保存，第一个节点保存元素的成员（member），而第二个元素
则保存元素的分值（score）。
压缩列表内的集合元素按分值从小到大进行排序，分值较小的元素被放置在靠近表头的
方向，而分值较大的元素则被放置在靠近表尾的方向。
举个例子，如果我们执行以下ZADD命令，那么服务器将创建一个有序集合对象作为
price键的值：
redis> zADD price 8.5 apple 5.0 banana 6.0 cherry
(integer)3
如果price键的值对象使用的是ziplist
编码，那么这个值对象将会是图8-14所示的样子，
redisObject
而对象所使用的压缩列表则会是8-15所示的样子。
type
REDIS_ZSET
skiplist编码的有序集合对象使用zset结
encoding
构作为底层实现，一个zset结构同时包含一个字
REDIS_ENCODING_ZIPLIST
典和一个跳跃表：
ptr
压缩列表
typedef struct zset (
zskiplist*zsl;
图8-14ziplist编码的有序集合对象