第18章发布与订阅303
18.4.3PUBSUBNUMPAT
PUBSUBNUMPAT子命令用于返回服务器当前被订阅模式的数量。
这个子命令是通过返回pubsub_patterns链表的长度来实现的，因为这个链表的长
度就是服务器被订阅模式的数量，这个过程可以用以下伪代码来描述：
def pubsub_numpat():
#pubsub_patterns链表的长度就是被订阅模式的数量
reply_pattern_count(len(server.pubsub_patterns))
redisServer
pubsubPattern
pubsubPattern
pubsubPattern
client
client
client
client-7
client-8
pubsub_patterns
client-9
pattern
pattern
pattern
"music.*"
"book.*"
"news.*"
图18-20pubsub_patterns链表
举个例子，对于图18-20所示的pubsub_patterns链表来说，执行PUBSUBNUMPAT
命令将返回3：
redis>PUBSUB NUMPAT
redisServer
(integer)3
pubsubPattern
而对于图18-21所示的pubsub_patterns
client
pubsub_patterns
client-7
链表来说，执行PUBSUBNUMPAT命令将返回1：
pattern
redis>PUBSUB NUMPAT
"music,*"
(integer)1
图18-21pubsub_patterns链表
18.5
重点回顾
口服务器状态在pubsubchannels字典保存了所有频道的订阅关系：SUBSCRIBE
命令负责将客户端和被订阅的频道关联到这个字典里面，而UNSUBSCRIBE命令则
负责解除客户端和被退订频道之间的关联。
口服务器状态在pubsub_patterns链表保存了所有模式的订阅关系：PSUBSCRIBE
命令负责将客户端和被订阅的模式记录到这个链表中，而PUNSUBSCRIBE命令则负
责移除客户端和被退订模式在链表中的记录。
口PUBLISH命令通过访问pubsub_channels字典来向频道的所有订阅者发送消息，
通过访问pubsub_patterns链表来向所有匹配频道的模式的订阅者发送消息。
口PUBsUB命令的三个子命令都是通过读取pubsub_channels字典和pubsub
patterns链表中的信息来实现的。