366·第四部分独立功能的实现
3）定位到buf【01字节上面，然后取出该字节上的第4个二进制位（从左向右数）的值。
4）向客户端返回二进制位的值1。
命令的执行过程如图22-4所示。
1)定位到buf[0]字节
buf[0]
2)返回第4个二进制位的值
buf[1]】（空字符）
图22-4查找并返回offset为3的二进制位的过程
再举一个例子，对于图22-3所示的位数组来说，命令：
GETBIT<bitarray>10
将执行以下操作：
1）108」的值为1。
2）（10mod8）+1的值为3。
3）定位到buf【1]字节上面，然后取出该字节上的第3个二进制位的值。
4）向客户端返回二进制位的值0。
命令的执行过程如图22-5所示。
buf[0]
1）定位到buf【1]字节
buf(1]
buf[2]
2）返回第3个二进制位的值
buf[3]
图22-5查找并返回ofset为10的二进制位的过程
因为GETBIT命令执行的所有操作都可以在常数时间内完成，所以该命令的算法复杂度
为 0(1)。
22.3
SETBIT命令的实现
SETBIT用于将位数组bitarray在offset偏移量上的二进制位的值设置为value，
并向客户端返回二进制位被设置之前的旧值：
SETBIT<bitarray><offset><value>
以下是SETBIT命令的执行过程：
1）计算len=Loffset+8」+1，1en值记录了保存offset偏移量指定的二进制位至少
需要多少字节。
2）检查bitarray键保存的位数组（也即是SDs）的长度是否小于len，如果是的话，
将SDS的长度扩展为1en字节，并将所有新扩展空间的二进制位的值设置为0。
3）计算byte=Lofset=8」，byte值记录了offset偏移量指定的二进制位保存在位数