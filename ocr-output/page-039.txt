22第一部分数据结构与对象
（续）
函数
作用
时间复杂度
链表长度可以通过链表的len
listLength
返回链表的长度（包含了多少个节点）
属性直接获得，O(1)
表头节点可以通过链表的head
listFirst
返回链表的表头节点
属性直接获得，O(1)
表尾节点可以通过链表的tail
listLast
返回链表的表尾节点
属性直接获得，O(1)
前置节点可以通过节点的prev
listPrevNode
返回给定节点的前置节点
属性直接获得，O(1)
后置节点可以通过节点的next
listNextNode
返回给定节点的后置节点
属性直接获得，O(1)
节点值可以通过节点的value
listNodeValue
返回给定节点目前正在保存的值
属性直接获得，O(1)
listCreate
创建一个不包含任何节点的新链表
0(1)
将一个包含给定值的新节点添加到给定链表
listAddNodeHead
0(1)
的表头
将一个包含给定值的新节点添加到给定链表
listAddNodeTail
0(1)
的表尾
将一个包含给定值的新节点添加到给定节点
listInsertNode
0(1)
的之前或者之后
listSearchKey
查找并返回链表中包含给定值的节点
O(M)，N为链表长度
listIndex
返回链表在给定索引上的节点
O(M)，N为链表长度
listDelNode
从链表中删除给定节点
O(M)，N为链表长度
将链表的表尾节点弹出，然后将被弹出的节
listRotate
0(1)
点插人到链表的表头，成为新的表头节点
listDup
复制一个给定链表的副本
O(M)，N为链表长度
释放给定链表，以及链表中的所有节点
listRelease
O(M)，N为链表长度
3.3
重点回顾
口链表被广泛用于实现Redis的各种功能，比如列表键、发布与订阅、慢查询、监视
器等。
口每个链表节点由一个1istNode结构来表示，每个节点都有一个指向前置节点和后
置节点的指针，所以Redis的链表实现是双端链表。
口每个链表使用一个1ist结构来表示，这个结构带有表头节点指针、表尾节点指针，
以及链表长度等信息。
口因为链表表头节点的前置节点和表尾节点的后置节点都指向NULL，所以Redis的链
表实现是无环链表。
口通过为链表设置不同的类型特定函数，Redis的链表可以用于保存各种不同类型的值。