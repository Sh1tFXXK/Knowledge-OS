第14章服务器185
//保存了秒级精度的系统当前UNIX时间戳
time_t unixtime;
//保存了毫秒级精度的系统当前UNIX时间戳
long long mstime;
因为serverCron函数默认会以每1oo毫秒一次的频率更新unixtime属性和
mstime属性，所以这两个属性记录的时间的精确度并不高：
口服务器只会在打印日志、更新服务器的LRU时钟、决定是否执行持久化任务、计算
服务器上线时间（uptime）这类对时间精确度要求不高的功能上。
口对于为键设置过期时间、添加慢查询日志这种需要高精确度时间的功能来说，服务
器还是会再次执行系统调用，从而获得最准确的系统当前时间。
14.2.2
更新LRU时钟
服务器状态中的1ruclock属性保存了服务器的LRU时钟，这个属性和上面介绍的
unixtime属性、mstime属性一样，都是服务器时间缓存的一种：
structredisserver(
//默认每10秒更新一次的时钟缓存，
/／用于计算键的空转（idle）时长。
unsignedlruclock:22;
每个Redis对象都会有一个1ru属性，这个1ru属性保存了对象最后一次被命令访问
的时间：
typedefstructredisobject
unsigned lru:22;
1/ ...
I robj;
当服务器要计算一个数据库键的空转时间（也即是数据库键对应的值对象的空转时间），
程序会用服务器的1ruclock属性记录的时间减去对象的1ru属性记录的时间，得出的计