370→第四部分独立功能的实现
BITCOUNT命令要做的工作初看上去并不复杂，但实际上要高效地实现这个命令并不
容易，需要用到一些精巧的算法。
接下来的几个小节将对BITCOUNT命令可能使用的几种算法进行介绍，并最终给出
BITCOUNT命令的具体实现原理。
22.4.1二进制位统计算法（1）：遍历算法
实现BITCOUNT命令最简单直接的方法，就
counter=l
是遍历位数组中的每个二进制位，并在遇到值为
buf[0]
1的二进制位时，将计数器的值增一。
buf[1］（空字符）
图22-17展示了程序使用遍历算法，对一个8
位长的位数组进行遍历并计数的整个过程。
counter=l
遍历算法虽然实现起来简单，但效率非常低，
buf[0]
因为这个算法在每次循环中只能检查一个二进制
buf【1]（空字符）
位的值是否为1，所以检查操作执行的次数将与
位数组包含的二进制位的数量成正比。
counter=2
例如，假设要检查的位数组的长度为100
MB,
buf[0]
那么按1MB=1000000Byte=8000000
buf[1］（空字符）
bit来计算，使用遍历算法检查长度为100MB的
counter=3
位数组将需要执行检查操作八亿次（100*8000
buf[0]
000）！而对于长度为500MB的位数组来说，遍
buf[1]（空字符)
历算法将需要执行检查操作四十亿次！
尽管遍历算法对单个二进制位的检查可以在
counter=3
很短的时间内完成，但重复执行上亿次这种检
buf[0]
查肯定不是一个高效程序应有的表现，为了让
buf[1]（空字符）
BITCOUNT命令的实现尽可能地高效，程序必须
counter=3
尽可能地增加每次检查所能处理的二进制位的数
buf[0]
量，从而减少检查操作执行的次数。
buf[1]（空字符）
22.4.2
二进制位统计算法（2）：查表
counter=4
算法
buf[0]
优化检查操作的一个办法是使用查表法：
buf[1]（空字符）
口对于一个有限集合来说，集合元素的排列
counter=4
方式是有限的。
buf [0]
口而对于一个有限长度的位数组来说，它能
buf【1]（空字符）
表示的二进制位排列也是有限的。
图22-17
根据这个原理，我们可以创建一个表，表的
遍历算法的运行过程