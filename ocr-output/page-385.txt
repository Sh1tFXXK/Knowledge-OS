368→第四部分
独立功能的实现
SETBIT<bitarray>121
那么服务器将执行以下操作：
1）计算L128」+1，得出值2，这表示保存偏移量为12的二进制位至少需要2字节
长的位数组。
redisObject
2）对位数组的长度
进行检查，得知位数组现
type
在的长度为1字节，这比
REDIS_STRING
执行命令所需的最小长度
sdshdr
2字节要小，所以程序会
free
要求将位数组的长度扩展
buf[o]
为2字节。不过，尽管程
len
buf[1]
序只要求2字节长的位数
buf
buf[2]（空字符）
组，但SDS的空间预分配
buf[3]（未使用）
策略会为SDS额外多分配
buf[4]（未使用）
2字节的未使用空间，再
加上为保存空字符而额外
图22-8扩展空间之后的位数组
分配的1字节，扩展之后
buf数组的实际长度为5字节，如图
1）定位到buf[1]字节
2）定位到buf[1]字节的第5个二进制位
22-8所示。
首先将位现在的值o保存到oldvalue
变量然后将位的值设置为1
3）计算L12=8」，得出值1，
buf[0]
说明偏移量为12的二进制位位于
buf[1]】字节中。
buf[1]
4）计算（12mod8）+1，得出
buf【2］（空字符）
值5，说明偏移量为12的二进制位
buf【3］（未使用）
是buf[1]】字节的第5个二进制位。
buf【4]（未使用）
5）定位到buf【1]字节的第5
图22-9
个二进制位，将二进制位现在的值
SETBIT命令的执行过程
o保存到oldvalue变量，然后将
二进制位的值设置为1。
buf[0]
6）向客户端返回oldvalue变量的值0。
buf[1]
图22-9.展示了SETBIT命令定位并设置指定二
进制位的过程，而图22-10则展示了SETBIT命令
buf[2]（空字符）
执行之后，位数组的样子。
buf[3]（未使用）
注意，因为buf数组使用逆序来保存位数组，
buf【4]（未使用）
所以当程序对buf数组进行扩展之后，写入操作可
以直接在新扩展的二进制位中完成，而不必改动位
图22-10执行SETBIT命令之后的位数组
数组原来已有的二进制位。
相反地，如果buf数组使用和书写位数组时一样的顺序来保存位数组，那么在每次扩展