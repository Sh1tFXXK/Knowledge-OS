第7章压缩列表53
及用途。
zllen
entryl
zlbytes
zltail
entry2
entryN
zlend
图7-1
压缩列表的各个组成部分
表 7-1
压缩列表各个组成部分的详细说明
属性
类型
长度
用途
记录整个压缩列表占用的内存字节数：在对压缩列表进行内存重分配，
uint32_t
zlbytes
4字节
或者计算zlend的位置时使用
记录压缩列表表尾节点距离压缩列表的起始地址有多少字节：通过这个
uint32_t
zltail
4字节
偏移量，程序无须遍历整个压缩列表就可以确定表尾节点的地址
记录了压缩列表包含的节点数量：当这个属性的值小于UINT16MAX
2字节
uint16_t
zllen
（65535）时，这个属性的值就是压缩列表包含节点的数量；当这个值等于
UINT16MAX时，节点的真实数量需要遍历整个压缩列表才能计算得出
列表节点
不定
entryx
压缩列表包含的各个节点，节点的长度由节点保存的内容决定
uint8_t
zlend
1字节
特殊值0xFF（十进制255），用于标记压缩列表的末端
图7-2展示了一个压缩列表示例：
口列表z1bytes属性的值为0x50（十进制80），表示压缩列表的总长为80字节。
口列表z1tai1属性的值为0x3c（十进制60），这表示如果我们有一个指向压缩列
表起始地址的指针P，那么只要用指针p加上偏移量60，就可以计算出表尾节点
entry3的地址。
口列表z11en属性的值为0x3（十进制3），表示压缩列表包含三个节点。
zltail
zlbytes
zllen
zlend
entryl
entry2
entry3
0x50
0x3c
0x3
OxFF
p+60
图7-2包含三个节点的压缩列表
图7-3展示了另一个压缩列表示例：
zlbytes
zltail
zllen
zlend
entryl
entry4
entry2
entry3
entry5
Oxd2
0xb3
0x5
OxFF
p+179
图7-3包含五个节点的压缩列表
口列表z1bytes属性的值为0xd2（十进制210），表示压缩列表的总长为210字节。
口列表z1tai1属性的值为0xb3（十进制179），这表示如果我们有一个指向压缩列
表起始地址的指针P，那么只要用指针p加上偏移量179，就可以计算出表尾节点
entry5的地址。
口列表z11en属性的值为0x5（十进制5），表示压缩列表包含五个节点。