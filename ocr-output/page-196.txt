第14章服务器，179
表14-2列出了sf1ags属性可以使用的标识值，以及这些标识的意义。
表14-2
sflags属性的标识
标识
意义
带有这个标识的命令
这是一个写入命令，可能会修改数据库
SET、RPUSH、DEL等等
这是一个只读命令，不会修改数据库
GET、STRLEN、EXISTS等等
这个命令可能会占用大量内存，执行之前
SET、APPEND、RPUSH、LPUSH、SADD、
需要先检查服务器的内存使用情况，如果
SINTERSTORE等等
内存紧缺的话就禁止执行这个命令
这是一个管理命令
SAVE、BGSAVE、SHUTDOWN等等
这是一个发布与订阅功能方面的命令
PUBLISH、SUBSCRIBE、PUBSUB等等
这个命令不可以在Lua脚本中使用
BRPOP、BLPOP、BRPOPLPUSH、SPOP等等
这是一个随机命令，对于相同的数据集和
SPOP、SRANDMEMBER、SSCAN、
相同的参数，命令返回的结果可能不同
RANDOMKEY等等
当在Lua脚本中使用这个命令时，对这
SINTER、SUNION、SDIFF、SMEMBERS、
个命令的输出结果进行一次排序，使得命
KEYS等等
令的结果有序
这个命令可以在服务器载入数据的过程中
INFO、SHUTDOWN、PUBLISH等等
使用
这是一个允许从服务器在带有过期数据时
SLAVEOF、PING、INFO等等
使用的命令
这个命令在监视器（monitor）模式下不会
EXEC
自动被传播（propagate）
图14-4展示了命令表的样子，并且以SET命令和GET命令作为例子，展示了redisCommand
结构：
口SET命令的名字为"set"，实现函数为setCommand；命令的参数个数为-3，表
示命令接受三个或以上数量的参数；命令的标识为"wm"，表示SET命令是一个写
人命令，并且在执行这个命令之前，服务器应该对占用内存状况进行检查，因为这
个命令可能会占用大量内存。
口GET命令的名字为"get"，实现函数为getCommand函数；命令的参数个数为2，
表示命令只接受两个参数；命令的标识为"r"，表示这是一个只读命令。
继续之前SET命令的例子，当程序以图14-3中的argv[0】作为输人，在命令表中进
行查找时，命令表将返回"set"键所对应的redisCommand结构，客户端状态的cmd指
针会指向这个redisCommand结构，如图14-5所示。