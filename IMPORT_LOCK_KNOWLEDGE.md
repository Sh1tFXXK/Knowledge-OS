# MySQL 锁机制知识导入指南

## 📦 知识内容概览

本次导入包含完整的 MySQL 锁机制知识体系，按照 Knowledge OS 的设计思想组织：

### 🎯 知识节点（9个）

#### 公理区（定义/常识）
1. **共享锁（读锁）** - S 锁，允许多个事务同时读取
2. **排他锁（写锁）** - X 锁，独占访问

#### 机制区（过程/方法）
3. **锁机制** - 核心概念，并发控制的基础
4. **表级锁** - MyISAM 默认，粒度最大
5. **行级锁** - InnoDB 默认，粒度最小，包含 Record/Gap/Next-Key Lock
6. **页面锁** - BDB 支持，介于表锁和行锁之间
7. **乐观锁** - 应用层冲突检测，版本号/时间戳机制
8. **悲观锁** - 数据库层锁机制，共享锁和排他锁属于此类

#### 结论区（性质/能力）
9. **锁粒度对比** - 表锁 vs 行锁 vs 页锁的多维对比
10. **存储引擎锁支持** - InnoDB/MyISAM/MEMORY/BDB 的锁支持情况

### 🔗 知识关系（15条）

建立了完整的关系网络：
- `锁机制` 包含各种具体锁类型
- `共享锁/排他锁` 属于 `悲观锁`
- 各种锁参与 `锁粒度对比`
- `InnoDB` 支持 `行级锁`
- `行级锁` 可能导致 `死锁`
- 等等...

### ❓ 问题库（10个）

按不同维度拆分的问题，每个都带答案和关联节点：

**存储引擎维度**
- MySQL 不同存储引擎分别支持什么锁？
- InnoDB 默认用的是行锁还是表锁？

**操作类型维度**
- 共享锁（读锁）和排他锁（写锁）的区别是什么？
- 为什么多个读锁可以共存，但写锁会阻断其他锁？

**粒度维度**
- 表级锁、行级锁、页面锁在开销、死锁、并发度上如何对比？
- 哪种锁粒度最容易发生锁冲突？哪种并发度最高？

**性能/策略维度**
- 乐观锁和悲观锁的本质区别是什么？
- 乐观锁通常如何实现冲突检测？
- 共享锁和排他锁属于乐观锁还是悲观锁？
- 行级锁为什么并发度高但可能死锁？

## 🚀 导入方法

### 方法一：浏览器控制台导入（推荐）

1. 启动开发服务器：
   ```bash
   npm run dev
   ```

2. 在浏览器中打开应用（默认 http://localhost:5176）

3. 打开浏览器控制台（F12 或右键 → 检查 → Console）

4. 执行导入命令：
   ```javascript
   await window.importLockKnowledge()
   ```

5. 等待控制台显示导入成功信息：
   ```
   🚀 开始导入 MySQL 锁机制知识...
   📦 导入知识节点...
   ✅ 导入了 9 个知识节点
   🔗 导入知识关系...
   ✅ 导入了 15 条知识关系
   ❓ 导入问题...
   ✅ 导入了 10 个问题
   🌳 更新宇宙目录树...
   ✅ 添加了锁机制目录节点
   💾 数据已持久化到 localStorage
   🎉 导入完成！
   ```

6. 刷新页面，在左侧目录树查看：
   ```
   知识宇宙
   └─ 计算机科学
      └─ 数据库
         └─ MySQL
            └─ 事务
               ├─ ACID
               ├─ 隔离级别
               ├─ MVCC
               └─ 锁机制 ✨ 新增
                  ├─ 共享锁
                  ├─ 排他锁
                  ├─ 表级锁
                  ├─ 行级锁
                  ├─ 页面锁
                  ├─ 乐观锁
                  ├─ 悲观锁
                  ├─ 锁粒度对比
                  └─ 引擎锁支持
   ```

### 方法二：手动导入（开发调试）

如果需要修改数据或调试，可以：

1. 编辑 `import-lock-knowledge.json` 修改知识内容
2. 编辑 `src/utils/importLockKnowledge.ts` 修改导入逻辑
3. 重新执行导入命令

## 📊 导入后的体验

### 1. 左侧目录树
- 在 `MySQL → 事务 → 锁机制` 下展开查看所有节点
- 点击节点查看解释卡（定义/机制/边界/来源）

### 2. 中间推理漏斗
- 选中锁相关节点，查看推理路径
- 按不同维度（事务视角/存储视角/性能视角）切换

### 3. 右侧关系网络
- 可视化查看锁机制的知识图谱
- 查看节点间的 `包含/属于/对比/支持` 等关系

### 4. 问题库
- 右下角问题库显示 10 个新问题
- 点击问题自动跳转到关联节点
- 点击 💬 图标查看答案
- 点击 ▶ 答案 展开/折叠答案内容

### 5. 问题数据库
- 顶部切换到"问题数据库"视图
- 表格模式：看到"关联知识点"列和"答案"列
- 卡片模式：每个问题显示关联节点和答案预览
- 搜索、过滤、分组功能完整可用

## 🎨 知识组织思想

本次导入完全遵循 Knowledge OS 的设计理念：

### 分层推理结构
```
公理区（是什么）
   ↓ leads-to
机制区（怎么做）
   ↓ enables
结论区（有什么用）
```

### 网状关联
- 节点池存储知识实体（是什么）
- 边表存储关系（连着谁）
- 目录树仅导航（怎么找）
- 问题库作为检索入口（怎么问）

### 多维视角
每个节点标注 dimensions：
- `transaction` - 事务视角：隔离性、ACID 相关
- `storage` - 存储视角：引擎实现、物理结构
- `performance` - 性能视角：并发度、开销、冲突

### 问题驱动
- 问题拆分到可验证粒度
- 每个问题关联具体节点
- 答案结构化（对比表、列表、流程）

## 🔄 数据持久化

导入的数据会自动保存到浏览器 localStorage：
- **Key**: `knowledge-os:app-state-v1`
- **内容**: 包含全部节点、边、问题、目录树
- **持久化**: 刷新页面后数据依然存在

如需清空数据重新导入：
```javascript
// 清空所有数据
localStorage.removeItem('knowledge-os:app-state-v1')
// 刷新页面
location.reload()
// 重新导入
await window.importLockKnowledge()
```

## 📝 扩展建议

如需添加更多锁相关知识（如死锁、间隙锁、Next-Key Lock 等）：

1. 在 `import-lock-knowledge.json` 中添加节点：
   ```json
   {
     "id": "deadlock",
     "label": "死锁",
     "role": "mechanism",
     "dimensions": ["transaction"],
     "card": { ... }
   }
   ```

2. 添加关系：
   ```json
   {
     "source": "row_lock",
     "target": "deadlock",
     "type": "leads-to",
     "label": "可能导致",
     "dimensions": ["transaction"]
   }
   ```

3. 添加问题：
   ```json
   {
     "text": "什么情况下会发生死锁？",
     "relatedNodeId": "deadlock",
     "answer": "..."
   }
   ```

4. 重新执行导入即可

## 🐛 故障排查

### 导入失败
- 检查 `import-lock-knowledge.json` 是否在 public 目录或项目根目录
- 检查控制台是否有 JSON 解析错误
- 确认 `window.importLockKnowledge` 函数已定义

### 数据未显示
- 刷新页面
- 检查 localStorage 中是否有数据
- 清空缓存重新导入

### 关系网络未显示
- 检查节点的 dimensions 是否正确
- 切换不同的维度视角试试
- 查看控制台是否有错误

## 📚 参考资料

本知识内容参考：
- 《MySQL 技术内幕：InnoDB 存储引擎》
- 《高性能 MySQL》
- MySQL 官方文档
- 数据库系统概念
- InnoDB 源码（lock0lock.cc, row0sel.cc 等）
