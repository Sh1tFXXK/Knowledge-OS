# 节点数据库功能 - Obsidian 风格

## 功能概述

创建了一个类似 Obsidian 数据库的节点库管理功能，支持搜索、过滤、排序、分组和多种视图模式。

## 核心功能

### ✅ 1. 双视图模式
- **表格视图** (☰)：类似电子表格，适合批量查看和比较
- **卡片视图** (▦)：类似看板，适合浏览和快速预览

### ✅ 2. 强大的搜索和过滤
- **全文搜索**：搜索节点名称、标题、内容
- **角色过滤**：公理、机制、结论、子系统
- **维度过滤**：事务、存储、性能等

### ✅ 3. 灵活的排序
- **按名称排序**：字母顺序
- **按角色排序**：分类排序
- **按维度数量排序**：多维度节点优先
- **升序/降序切换**：点击表头切换

### ✅ 4. 智能分组
- **不分组**：平铺显示所有节点
- **按角色分组**：公理、机制、结论、子系统
- **按维度分组**：按第一个维度分组
- **按共享状态分组**：共享节点 vs 普通节点

### ✅ 5. 快速操作
- **点击节点**：跳转到知识图谱
- **删除节点**：带确认对话框
- **实时统计**：显示当前筛选结果数量

## 文件结构

### 新增文件

1. **`src/components/NodeDatabase.tsx`** - 主组件
   - 搜索和过滤逻辑
   - 排序和分组逻辑
   - 表格和卡片视图渲染

2. **`src/styles/database.css`** - 样式文件
   - 工具栏样式
   - 表格视图样式
   - 卡片视图样式
   - 角色徽章和维度标签

### 修改文件

1. **`src/App.tsx`**
   - 导入 NodeDatabase 组件
   - 导入 database.css
   - 添加视图切换逻辑

2. **`src/layout/TopBar.tsx`**
   - 添加"数据库"视图选项 (🗄️)

3. **`src/styles/layout.css`**
   - 添加 `.center-full` 全屏布局样式

## 使用指南

### 访问数据库
1. 点击顶部导航栏的 **🗄️ 数据库** 按钮
2. 进入节点数据库视图

### 搜索节点
1. 在搜索框输入关键词
2. 实时过滤显示匹配的节点

### 过滤和筛选
1. **角色过滤**：选择公理/机制/结论/子系统
2. **维度过滤**：选择事务/存储/性能
3. 多个过滤条件同时生效

### 排序
1. 点击表格表头（名称、角色、维度）
2. 再次点击切换升序/降序
3. 表头显示 ↑ 或 ↓ 指示当前排序

### 分组查看
1. 选择分组方式（按角色/按维度/按共享）
2. 节点按组显示，每组显示数量
3. 选择"不分组"恢复平铺视图

### 切换视图
1. 点击工具栏右侧的 ☰ (表格) 或 ▦ (卡片)
2. 表格视图：适合数据对比
3. 卡片视图：适合内容浏览

### 节点操作
1. **点击节点**：跳转到知识图谱并聚焦
2. **点击删除按钮** (🗑)：删除节点（需确认）

## 界面布局

### 工具栏
```
[搜索框] [角色过滤] [维度过滤] [分组方式]  [☰表格] [▦卡片] [共N个节点]
```

### 表格视图
```
┌────────────┬────────┬────────────┬────────┬────────┐
│ 名称 ↑     │ 角色   │ 维度       │ 标签   │ 操作   │
├────────────┼────────┼────────────┼────────┼────────┤
│ ⟳ SQL语句  │ 公理   │ transaction│ -      │ 🗑     │
│ MVCC       │ 机制   │ transaction│ -      │ 🗑     │
│ B+树索引   │ 机制   │ storage    │ -      │ 🗑     │
└────────────┴────────┴────────────┴────────┴────────┘
```

### 卡片视图
```
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│ ⟳ SQL语句   🗑  │ │ MVCC        🗑  │ │ B+树索引    🗑  │
│                 │ │                 │ │                 │
│ [公理]          │ │ [机制]          │ │ [机制]          │
│ transaction     │ │ transaction     │ │ storage         │
│ storage         │ │ performance     │ │ performance     │
│                 │ │                 │ │                 │
│ 结构化查询语... │ │ 多版本并发控... │ │ 平衡多路搜索... │
└─────────────────┘ └─────────────────┘ └─────────────────┘
```

## 分组展示示例

### 按角色分组
```
━━━ 公理 (3) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  SQL语句, 事务, ACID

━━━ 机制 (8) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  MVCC, Undo Log, 版本链, Read View, ...

━━━ 结论 (2) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  快照读, 查询优化

━━━ 子系统 (1) ━━━━━━━━━━━━━━━━━━━━━━━━━
  InnoDB
```

## 技术实现

### 搜索算法
```typescript
// 全文搜索：节点名称、标题、内容
const filteredNodes = nodes.filter((node) =>
  node.label.toLowerCase().includes(query) ||
  node.card?.title?.toLowerCase().includes(query) ||
  node.card?.tabs?.some(tab => 
    tab.content.toLowerCase().includes(query)
  )
);
```

### 排序算法
```typescript
sorted.sort((a, b) => {
  let compareValue = 0;
  
  switch (sortBy) {
    case 'name':
      compareValue = a.label.localeCompare(b.label, 'zh-CN');
      break;
    case 'role':
      compareValue = (a.role || '').localeCompare(b.role || '');
      break;
    case 'dimensions':
      compareValue = (a.dimensions?.length || 0) - 
                     (b.dimensions?.length || 0);
      break;
  }
  
  return sortOrder === 'asc' ? compareValue : -compareValue;
});
```

### 分组算法
```typescript
const groups: Record<string, Node[]> = {};

nodes.forEach((node) => {
  let groupKey = '未分类';
  
  switch (groupBy) {
    case 'role':
      groupKey = roleToLabel(node.role);
      break;
    case 'dimension':
      groupKey = node.dimensions?.[0] || '无维度';
      break;
    case 'shared':
      groupKey = node.shared ? '共享节点' : '普通节点';
      break;
  }
  
  groups[groupKey].push(node);
});
```

## 与 Obsidian 对比

| 功能 | Obsidian | Knowledge OS |
|------|----------|--------------|
| 表格视图 | ✅ | ✅ |
| 卡片视图 | ✅ | ✅ |
| 搜索过滤 | ✅ | ✅ |
| 排序 | ✅ | ✅ |
| 分组 | ✅ | ✅ |
| 内联编辑 | ✅ | ⏳ (未来) |
| 自定义字段 | ✅ | ⏳ (未来) |
| 公式计算 | ✅ | ⏳ (未来) |

## 性能优化

1. **useMemo 缓存**：搜索、过滤、排序、分组结果都使用 useMemo
2. **虚拟滚动**：未来可添加，当节点数量 > 1000 时
3. **防抖搜索**：未来可添加，减少实时搜索的计算

## 未来扩展

### 短期
- [ ] 批量操作（批量删除、批量修改角色）
- [ ] 导出功能（CSV、JSON、Markdown）
- [ ] 列宽调整
- [ ] 列显示/隐藏

### 中期
- [ ] 内联编辑（双击单元格编辑）
- [ ] 自定义字段（用户自定义属性）
- [ ] 高级过滤（组合条件、正则表达式）
- [ ] 保存视图配置

### 长期
- [ ] 公式和计算字段
- [ ] 图表统计
- [ ] 关系可视化
- [ ] 协作和分享

## 修改日期
2026年6月3日
