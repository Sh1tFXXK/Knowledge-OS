# 🎯 MySQL 锁机制知识 - 多维视角版本

## ✅ 正确理解 Knowledge OS 的设计

### 核心概念

1. **维度（Dimensions）≠ 答案中的分类**
   - 维度是用来**切换视角**的，不是写在答案里的
   - 切换维度后，关系网络只显示该维度相关的节点和边

2. **节点（Nodes）= 独立的知识实体**
   - 每个锁类型是独立的节点，不是列在一个答案里
   - 每个节点标注它所属的维度

3. **问题应该细粒度**
   - ❌ 错误："请说一下数据库锁的种类？"（把所有分类写在一个答案里）
   - ✅ 正确："什么是共享锁？""什么是行级锁？"（每个问题关联具体节点）

## 🔮 锁相关的维度视角

1. **🟠 操作类型（Operation Type）**
   - 共享锁（读锁）
   - 排他锁（写锁）

2. **🟣 锁粒度（Granularity）**
   - 表级锁
   - 行级锁
   - 页面锁

3. **🟡 锁策略（Strategy）**
   - 乐观锁
   - 悲观锁

4. **🔵 存储引擎（Storage Engine）**
   - InnoDB 支持的锁
   - MyISAM 支持的锁
   - MEMORY 支持的锁

加上系统内置的：
- **事务视角（Transaction）**
- **存储视角（Storage）**
- **性能视角（Performance）**

## 🚀 数据驱动架构

Knowledge OS 采用**数据与代码分离**的架构。知识数据存储在 `data/` 目录下的 JSON 文件中：

- `node-pool.json`: 存储所有知识节点（定义、机制、边界、来源）
- `knowledge-edges.json`: 存储节点间的关系
- `tree-data.json`: 存储目录结构
- `questions.json`: 存储问题库

系统启动时会自动加载这些数据。如需新增知识，只需编辑对应的 JSON 文件，无需修改代码。

## 🎨 体验多维视角

### 左侧目录树底部找到"🔮 多维视图 (Perspectives)"

点击展开，会看到系统内置及导入的维度视角。

### 切换维度的效果

#### 切换到"操作类型"视角
点击左侧目录树中的"锁机制"节点，右下角关系网络会显示：
- ✅ 锁机制 → 共享锁
- ✅ 锁机制 → 排他锁
- ✅ 共享锁 → 悲观锁
- ✅ 排他锁 → 悲观锁
- ❌ 其他维度的节点被过滤掉

## 📝 问题库验证

问题库包含细粒度问题，每个关联具体节点。点击问题后：
- 自动跳转到关联的节点
- 右上角显示节点的解释卡（定义/机制/边界/来源）
- 右下角关系网络显示该节点的关系

## 💡 设计思想总结

**Knowledge OS 的核心是"多维网状知识"：**

1. **节点池（Node Pool）**：知识实体的集合
2. **边表（Edges）**：知识之间的关系
3. **目录树（Tree）**：导航结构（仅引用节点池）
4. **维度（Dimensions）**：过滤镜头，切换不同视角
5. **问题库（Questions）**：知识检索入口

这样用户可以：
- 从不同维度理解同一个知识体系
- 点击问题直达具体节点
- 在关系网络中看到知识连接
- 通过推理漏斗理解知识层次
