# 🎯 MySQL 锁机制知识 - 多维视角版本

## ✅ 正确理解 Knowledge OS 的设计

### 核心概念

1. **维度（Dimensions）≠ 答案中的分类**
   - 维度是用来**切换视角**的，不是写在答案里的
   - 切换维度后，关系网络只显示该维度相关的节点和边

2. **节点（Nodes）= 独立的知识实体**
   - 每个锁类型是独立的节点，不是列在一个答案里
   - 每个节点标注它所属的维度

3. **问题应该细粒度**
   - ❌ 错误："请说一下数据库锁的种类？"（把所有分类写在一个答案里）
   - ✅ 正确："什么是共享锁？""什么是行级锁？"（每个问题关联具体节点）

## 🔮 新增的自定义维度

本次导入新增了 4 个锁相关的维度视角：

1. **🟠 操作类型（Operation Type）**
   - 共享锁（读锁）
   - 排他锁（写锁）

2. **🟣 锁粒度（Granularity）**
   - 表级锁
   - 行级锁
   - 页面锁

3. **🟡 锁策略（Strategy）**
   - 乐观锁
   - 悲观锁

4. **🔵 存储引擎（Storage Engine）**
   - InnoDB 支持的锁
   - MyISAM 支持的锁
   - MEMORY 支持的锁

加上系统内置的：
- **事务视角（Transaction）**
- **存储视角（Storage）**
- **性能视角（Performance）**

## 📦 导入内容（v2 版本）

- **4 个自定义维度**
- **9 个知识节点**（每个锁类型是独立节点）
- **13 条知识关系**
- **12 个问题**（细粒度问题，每个关联具体节点）

## 🚀 导入步骤

### 1. 访问应用
http://localhost:5173

### 2. 打开控制台（F12）

### 3. 执行导入
```javascript
await window.importLockKnowledge()
```

### 4. 查看输出
```
🚀 开始导入 MySQL 锁机制知识...
🔮 导入自定义维度...
✅ 导入了 4 个维度视角
   - 操作类型 (Operation Type): 按读写操作类型分类...
   - 锁粒度 (Granularity): 按锁定范围大小分类...
   - 锁策略 (Strategy): 按冲突处理策略分类...
   - 存储引擎 (Storage Engine): 按存储引擎支持情况分类...
📦 导入知识节点...
✅ 导入了 9 个知识节点
🔗 导入知识关系...
✅ 导入了 13 条知识关系
❓ 导入问题...
✅ 导入了 12 个问题
🌳 更新宇宙目录树...
✅ 添加了锁机制目录节点
💾 数据已持久化到 localStorage
🎉 导入完成！
```

### 5. 刷新页面

## 🎨 体验多维视角

### 左侧目录树底部找到"🔮 多维视图 (Perspectives)"

点击展开，会看到：

**系统内置维度：**
- 🌐 全部 (All)
- 🔴 事务视角 (Transaction)
- 🔵 存储视角 (Storage)
- 🟢 性能视角 (Performance)
- 🟡 安全视角 (Security)

**新增的锁机制维度：**
- 🟠 操作类型 (Operation Type)
- 🟣 锁粒度 (Granularity)
- 🟡 锁策略 (Strategy)
- 🔵 存储引擎 (Storage Engine)

### 切换维度的效果

#### 切换到"操作类型"视角
点击左侧目录树中的"锁机制"节点，右下角关系网络会显示：
- ✅ 锁机制 → 共享锁
- ✅ 锁机制 → 排他锁
- ✅ 共享锁 → 悲观锁
- ✅ 排他锁 → 悲观锁
- ❌ 其他维度的节点被过滤掉

#### 切换到"锁粒度"视角
关系网络会显示：
- ✅ 锁机制 → 表级锁
- ✅ 锁机制 → 行级锁
- ✅ 锁机制 → 页面锁
- ✅ 锁粒度对比
- ❌ 操作类型、策略相关的节点被过滤掉

#### 切换到"锁策略"视角
关系网络会显示：
- ✅ 乐观锁
- ✅ 悲观锁
- ✅ 共享锁 → 悲观锁
- ✅ 排他锁 → 悲观锁
- ❌ 粒度相关的节点被过滤掉

#### 切换到"性能"视角
关系网络会显示：
- ✅ 行级锁（高并发）
- ✅ 表级锁（低开销）
- ✅ 锁粒度对比
- ✅ 乐观锁（冲突检测）

## 📝 问题库验证

导入后，问题库会新增 12 个问题：

### 总览问题
1. ❓ MySQL 有哪些锁类型？ → 关联 `lock_mechanism`

### 操作类型维度
2. ❓ 什么是共享锁（读锁）？ → 关联 `shared_lock`
3. ❓ 什么是排他锁（写锁）？ → 关联 `exclusive_lock`
4. ❓ 共享锁和排他锁有什么区别？ → 关联 `shared_lock`

### 粒度维度
5. ❓ 什么是表级锁？ → 关联 `table_lock`
6. ❓ 什么是行级锁？ → 关联 `row_lock`
7. ❓ 表级锁和行级锁如何选择？ → 关联 `lock_granularity_comparison`

### 策略维度
8. ❓ 什么是乐观锁？ → 关联 `optimistic_lock`
9. ❓ 什么是悲观锁？ → 关联 `pessimistic_lock`
10. ❓ 乐观锁和悲观锁如何选择？ → 关联 `optimistic_lock`

### 引擎维度
11. ❓ InnoDB 支持哪些锁？ → 关联 `lock_engine_support`
12. ❓ MyISAM 支持哪些锁？ → 关联 `lock_engine_support`

**每个问题点击后：**
- 自动跳转到关联的节点
- 右上角显示节点的解释卡（定义/机制/边界/来源）
- 右下角关系网络显示该节点的关系
- 点击 💬 图标可以查看预填的答案

## 🎯 系统功能验证清单

导入后，验证以下功能：

### ✅ 1. 左侧目录树
- [ ] 展开 MySQL → 事务 → 锁机制
- [ ] 看到 9 个子节点（共享锁、排他锁、表级锁...）

### ✅ 2. 多维视图
- [ ] 左下角找到"🔮 多维视图"
- [ ] 点击展开看到 4 个新增维度
- [ ] 切换维度时关系网络过滤显示

### ✅ 3. 解释卡
- [ ] 点击任意锁节点
- [ ] 右上角显示解释卡，有 4 个 Tab（定义/机制/边界/来源）
- [ ] 内容完整，不是空的

### ✅ 4. 关系网络
- [ ] 右下角显示力导向图
- [ ] 节点按角色显示不同颜色（公理/机制/结论）
- [ ] 关系线按类型显示不同颜色
- [ ] 切换维度后只显示该维度的节点和边

### ✅ 5. 问题库
- [ ] 右下角问题库新增 12 个问题
- [ ] 点击问题跳转到关联节点
- [ ] 点击 💬 图标查看答案
- [ ] 答案内容完整

### ✅ 6. 问题数据库
- [ ] 顶部切换到"问题数据库"视图
- [ ] 表格模式显示"关联知识点"列（蓝色标签）
- [ ] 表格模式显示"答案"列（绿色 ✓ 已填写）
- [ ] 卡片模式显示完整信息

### ✅ 7. 数据持久化
- [ ] 刷新页面后数据仍然存在
- [ ] localStorage 中有 `knowledge-os:app-state-v1`

## 💡 设计思想总结

**Knowledge OS 的核心是"多维网状知识"：**

1. **节点池（Node Pool）**：知识实体的集合
2. **边表（Edges）**：知识之间的关系
3. **目录树（Tree）**：导航结构（仅引用节点池）
4. **维度（Dimensions）**：过滤镜头，切换不同视角
5. **问题库（Questions）**：知识检索入口

**不要：**
- ❌ 在一个答案里列举所有分类
- ❌ 把维度的概念写在答案里
- ❌ 问题太宽泛（"请说一下所有锁"）

**应该：**
- ✅ 每个概念是独立节点
- ✅ 用维度标注节点，通过切换视角过滤
- ✅ 问题细粒度，关联具体节点
- ✅ 用关系连接节点，形成知识网络

这样用户可以：
- 从不同维度理解同一个知识体系
- 点击问题直达具体节点
- 在关系网络中看到知识连接
- 通过推理漏斗理解知识层次

现在可以在浏览器中导入并体验完整的多维视角功能了！🎉
