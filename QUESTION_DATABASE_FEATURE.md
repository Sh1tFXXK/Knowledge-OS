# 问题库数据库功能 - 完整实现

## 功能概述

为问题库创建了一个类似 Obsidian 数据库的完整管理系统，支持搜索、过滤、排序、分组和双视图模式。

## 核心功能

### ✅ 1. 双视图模式
- **☰ 表格视图**：类似电子表格，三列布局（问题、状态、操作）
- **▦ 卡片视图**：看板式，适合浏览和快速操作

### ✅ 2. 强大的搜索和过滤
- **全文搜索**：搜索问题文本内容
- **状态过滤**：
  - ⭐ 未答复
  - ✅ 已答复
  - ❌ 忘答存疑

### ✅ 3. 灵活的排序
- **按问题文本排序**：字母顺序（支持中文）
- **按状态排序**：pending → answered → forgot
- **升序/降序切换**：点击表头切换

### ✅ 4. 智能分组
- **不分组**：平铺显示所有问题
- **按状态分组**：已答、未答、忘答
- **按关键词分组**：MVCC、SQL、索引、事务等

### ✅ 5. 完整的 CRUD 操作
- **查看**：列表显示所有问题
- **新增**：（在右侧面板问题库组件）
- **编辑**：内联编辑，点击 ✎ 编辑按钮
- **删除**：点击 🗑 删除按钮（需确认）
- **切换状态**：点击 🔄 按钮快速切换

### ✅ 6. 智能跳转
- **点击问题**：自动匹配相关知识节点并跳转到图谱

## 文件清单

### 新增文件
1. **`src/components/QuestionDatabase.tsx`** (350+ 行)
   - 主组件实现
   - 搜索、过滤、排序、分组逻辑
   - 表格和卡片视图

### 修改文件
1. **`src/App.tsx`**
   - 导入 QuestionDatabase 组件
   - 添加 questions 视图切换逻辑

2. **`src/layout/TopBar.tsx`**
   - 添加"❓ 问题库"视图选项

3. **`src/styles/database.css`**
   - 添加问题卡片样式
   - 添加状态徽章样式
   - 共享节点和问题的样式

## 使用指南

### 访问问题库数据库
1. 点击顶部导航栏的 **❓ 问题库** 按钮
2. 进入问题库数据库视图

### 搜索问题
1. 在搜索框输入关键词
2. 实时过滤显示匹配的问题

### 过滤和筛选
1. **状态过滤**：选择未答复/已答复/忘答存疑
2. 多个过滤条件同时生效

### 排序
1. 点击表格表头（问题、状态）
2. 再次点击切换升序/降序
3. 表头显示 ↑ 或 ↓ 指示当前排序

### 分组查看
1. 选择分组方式（按状态/按关键词）
2. 问题按组显示，每组显示数量
3. 选择"不分组"恢复平铺视图

### 切换视图
1. 点击工具栏右侧的 ☰ (表格) 或 ▦ (卡片)
2. 表格视图：适合批量操作
3. 卡片视图：适合浏览和查看详情

### 问题操作

#### 查看和跳转
- **点击问题**：自动跳转到相关知识节点

#### 切换状态
- **点击 🔄 按钮**：循环切换状态
  - 未答复 → 已答复 → 忘答存疑 → 未答复

#### 编辑问题
1. 点击 ✎ 编辑按钮
2. 修改问题文本
3. 按 Enter 保存或点击 ✓
4. 按 Escape 取消或点击 ✕

#### 删除问题
1. 点击 🗑 删除按钮
2. 确认删除对话框
3. 点击"确定"删除

## 界面布局

### 工具栏
```
[搜索框] [状态过滤] [分组方式]  [☰表格] [▦卡片] [共N个问题]
```

### 表格视图
```
┌─────────────────────────────┬──────────┬────────────┐
│ 问题 ↑                       │ 状态     │ 操作       │
├─────────────────────────────┼──────────┼────────────┤
│ 说说 MVCC 的实现原理？       │ ✅ 已答复│ 🔄 ✎ 🗑    │
│ 什么是 Read View？           │ ⭐ 未答复│ 🔄 ✎ 🗑    │
│ B+树和哈希索引的区别？       │ ✅ 已答复│ 🔄 ✎ 🗑    │
└─────────────────────────────┴──────────┴────────────┘
```

### 卡片视图
```
┌──────────────────────────────┐ ┌──────────────────────────────┐
│ ✅ 已答复        🔄 ✎ 🗑      │ │ ⭐ 未答复        🔄 ✎ 🗑      │
│                               │ │                               │
│ 说说 MVCC 的实现原理？        │ │ 什么是 Read View？            │
└──────────────────────────────┘ └──────────────────────────────┘
```

## 分组展示示例

### 按状态分组
```
━━━ ✅ 已答复 (3) ━━━━━━━━━━━━━━━━━━━━━━━━━━━
  说说 MVCC 的实现原理？
  B+树和哈希索引的区别是什么？
  InnoDB 和 MyISAM 有什么不同？

━━━ ⭐ 未答复 (4) ━━━━━━━━━━━━━━━━━━━━━━━━━━━
  什么是 Read View？
  RR 与 RC 在 MVCC 下的区别？
  什么情况下会发生死锁？
  Redo Log 和 Binlog 的区别？

━━━ ❌ 忘答存疑 (0) ━━━━━━━━━━━━━━━━━━━━━━━━
  (空)
```

### 按关键词分组
```
━━━ MVCC (2) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  说说 MVCC 的实现原理？
  RR 与 RC 在 MVCC 下的区别？

━━━ 索引 (1) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  B+树和哈希索引的区别是什么？

━━━ 其他问题 (3) ━━━━━━━━━━━━━━━━━━━━━━━━━━
  什么是 Read View？
  什么情况下会发生死锁？
  ...
```

## 技术实现

### 搜索算法
```typescript
const filteredQuestions = questions.filter((q) =>
  q.text.toLowerCase().includes(query.toLowerCase())
);
```

### 排序算法
```typescript
sorted.sort((a, b) => {
  switch (sortBy) {
    case 'text':
      return a.text.localeCompare(b.text, 'zh-CN');
    case 'status':
      const order = { pending: 1, answered: 2, forgot: 3 };
      return order[a.status] - order[b.status];
  }
});
```

### 分组算法
```typescript
// 按状态分组
const groups = {};
questions.forEach((q) => {
  const key = q.status === 'answered' ? '✅ 已答复'
    : q.status === 'forgot' ? '❌ 忘答存疑'
    : '⭐ 未答复';
  groups[key] = groups[key] || [];
  groups[key].push(q);
});

// 按关键词分组
const keywords = ['MVCC', 'SQL', '索引', '事务', 'InnoDB', 'B+树', '优化'];
const found = keywords.find(kw => q.text.includes(kw));
const key = found || '其他问题';
```

### 智能跳转
```typescript
// 匹配问题中的关键词到知识节点
const questionText = q.text.toLowerCase();
for (const [nodeId, node] of Object.entries(nodePool)) {
  if (questionText.includes(node.label.toLowerCase())) {
    setSelectedNode(nodeId); // 跳转到图谱
    break;
  }
}
```

## 与右侧面板的区别

| 功能 | 右侧面板问题库 | 问题库数据库视图 |
|------|---------------|----------------|
| 显示位置 | 右侧滚动面板 | 全屏中心区域 |
| 视图模式 | 列表视图 | 表格 + 卡片 |
| 搜索 | ❌ | ✅ |
| 过滤 | ❌ | ✅ |
| 排序 | ❌ | ✅ |
| 分组 | ❌ | ✅ |
| 批量查看 | 小范围 | 全屏大范围 |
| 新增问题 | ✅ | ❌ (在右侧面板) |
| 编辑/删除 | ✅ | ✅ |
| 适用场景 | 快速查看和添加 | 搜索、整理、批量管理 |

## 使用场景

### 场景 1：查找特定问题
```
用户：想找所有关于 MVCC 的问题
操作：
1. 点击顶部"❓ 问题库"
2. 搜索框输入"MVCC"
3. 查看过滤结果
```

### 场景 2：整理已答问题
```
用户：想查看哪些问题已经回答了
操作：
1. 点击顶部"❓ 问题库"
2. 状态过滤选择"已答复"
3. 查看所有已答问题
```

### 场景 3：分类管理问题
```
用户：想按主题组织问题
操作：
1. 点击顶部"❓ 问题库"
2. 分组方式选择"按关键词"
3. 按主题浏览问题
```

### 场景 4：批量编辑和删除
```
用户：需要清理不需要的问题
操作：
1. 点击顶部"❓ 问题库"
2. 切换到表格视图
3. 快速编辑或删除多个问题
```

## 性能优化

1. **useMemo 缓存**：搜索、过滤、排序、分组结果都使用 useMemo
2. **防抖搜索**：未来可添加，减少实时搜索的计算
3. **虚拟滚动**：未来可添加，当问题数量 > 500 时

## 未来扩展

### 短期
- [ ] 批量操作（批量删除、批量修改状态）
- [ ] 导出功能（CSV、Markdown）
- [ ] 问题标签系统
- [ ] 问题优先级

### 中期
- [ ] 问题关联（关联到多个知识节点）
- [ ] 问题笔记（添加详细答案）
- [ ] 学习进度追踪
- [ ] 复习提醒

### 长期
- [ ] AI 自动生成问题
- [ ] 智能推荐相关问题
- [ ] 学习路径规划
- [ ] 协作和分享

## 总结

问题库数据库功能提供了完整的问题管理解决方案，类似 Obsidian 的数据库功能，让用户可以：
- 🔍 快速搜索和定位问题
- 📊 多维度查看和组织问题
- ✏️ 方便地编辑和管理问题
- 🎯 智能跳转到相关知识节点

配合右侧面板的问题库，形成了"快速操作 + 深度管理"的完整工作流。

## 修改日期
2026年6月3日
